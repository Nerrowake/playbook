# Tests

The playbook is documentation-only at this time. There is no executable test
suite.

Changes should be verified by checking Markdown rendering, links, table
formatting, and consistency with the repository standards documented in
`docs/repository-standards.md`.
