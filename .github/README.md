# GitHub Configuration

This directory is reserved for repository-specific GitHub configuration.

The playbook currently relies on the organization defaults from
`Nerrowake/.github`. Add repository-specific workflows or templates here only
when the playbook needs behavior different from the organization baseline.
